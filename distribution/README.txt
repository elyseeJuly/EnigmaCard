隐信片本地网页包

这是一枚本地运行的隐信片工坊。

使用方式：
1. 先将构建后的静态网页文件放进 `app/` 目录
2. macOS 用户双击 `scripts/start-mac.command`
3. Windows 用户双击 `scripts/start-windows.bat`
4. 浏览器会打开本地地址，然后就可以开始使用

注意：
- 分享给别人时，建议将整个 `distribution/` 文件夹一起压缩发送
- 若用于正式部署，只需要把 `app/` 里的静态文件上传到静态托管即可
- 当前版本默认所有编码、解码与本地保存都在浏览器内完成
